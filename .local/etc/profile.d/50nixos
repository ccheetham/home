# =============================================================================
# NixOS
# -----------------------------------------------------------------------------
# https://nixos.org/
# =============================================================================

NIX_PROFILES_DIR=/nix/var/nix/profiles

[[ -d $NIX_PROFILES_DIR/default ]] || return

ME_PATH+=:$NIX_PROFILES_DIR/default/bin

# vim: ft=zsh
