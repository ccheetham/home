#!/usr/bin/env zsh

while [ 1 ]
do
    pwd=$(pwd)
    if [[ -f $pwd/gradlew ]]
    then
        exec $pwd/gradlew $@
    fi
    if [[ $pwd == / ]]
    then
        echo gradlew not found
        exit 1
    fi
    cd ..
done

# vim: ft=sh et sw=4 sts=4
