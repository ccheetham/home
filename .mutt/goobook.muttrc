# see ~/.goobookrc

set query_command = "goobook query %s"

# ----------------------------------------------------------------------------
# <tab> runs goobook query on alias
# 'a' adds sender to contacts
# ----------------------------------------------------------------------------
bind editor <tab> complete-query
macro index,pager a \
    "<pipe-message>goobook add<return>" \
    "add sender to Google contacts"

# vim: filetype=muttrc
