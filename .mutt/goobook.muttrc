# see ~/.goobookrc

set query_command="goobook query %s"
bind editor <Tab> complete-query

# add sender to "My Contacts"
macro index,pager a "<pipe-message>goobook add<return>" \
    "add sender to Google contacts"

# vim: filetype=muttrc
