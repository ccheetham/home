set folder = ~/var/mail/$MUTT_INSTANCE
set mbox_type = Maildir
set mbox = "[Gmail].All Mail"
set spoolfile = "+INBOX"
set postponed = "+[Gmail].Drafts"
set record    = "+[Gmail].Sent Mail"

# vim: filetype=muttrc
