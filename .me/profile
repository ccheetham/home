export ME_PROFILE_DIR=$(dirname $0)

export ME_FUNCTIONS=$ME_PROFILE_DIR/functions
source $ME_FUNCTIONS

source $ME_PROFILE_DIR/env

/bin/rm -f $ME_SHELL_LOG

for f in $ME_PROFILE_DIR/[[:digit:]]* ; do
    shlog $0 sourcing $f
    source $f
done

# vim: ft=zsh
