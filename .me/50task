# =============================================================================
# Taskwarrior
# -----------------------------------------------------------------------------
# http://taskwarrior.org/
# =============================================================================

checkexe task

t() {
    if [[ $# == 0 ]] ; then
        clear
        task
    else
        task $*
    fi
}

if [[ $ME_HOST == mini ]] ; then
    export TASKDDATA=$ME_VAR_DIR/taskd
fi

# vim: ft=zsh
