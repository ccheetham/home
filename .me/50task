checkexe task

alias t='task'
alias tl='t list'
alias ta='t add'
ts() {
    local filter=$1
    shift
    t $filter start $*
}
td() {
    local filter=$1
    shift
    t $filter done $*
}

# vim: ft=zsh
