# =============================================================================
# PostgreSQL
# -----------------------------------------------------------------------------
# http://www.postgresql.org/
# =============================================================================

export PGDATA=$ME_VAR_DIR/pg

ME_PG_CMD='postgres -d2'

alias pg="$ME_PG_CMD"
alias xpg="xterm -title PostgreSQL -name log -e $ME_PG_CMD &"

# vim: ft=zsh
