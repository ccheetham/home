if [[ $ME_DIST_NAME == darwin ]] ; then
    prepend_path /opt/X11
fi

export XSESSION_LOG=$ME_LOG_DIR/xsession.$(hostname).log

xtail() {
    tail -f $XSESSION_LOG
}

alias x='startx 2> $XSESSION_LOG'
alias xlog='xterm -T "X Session" -name log -e tail -f $XSESSION_LOG'

# vim: ft=zsh
