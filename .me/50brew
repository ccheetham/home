alias b='brew'

bsync() {
    b update
    b outdated
    echo -n 'Upgrade? '
    read
    b upgrade
    b linkapps cleanup
}

if [[ $ME_DIST_NAME == darwin ]]; then
    fpath=(/usr/local/share/zsh-completions $fpath)
fi

# vim: ft=zsh
