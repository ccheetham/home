alias b='brew'

bsync() {
    b update
    b outdated
    echo -n 'Upgrade? '
    read
    b upgrade
    b linkapps bleanup
}

if [[ $ME_DIST_NAME == darwin ]]; then
    fpath=(/usr/local/share/zsh-completions $fpath)
fi

# vim: ft=zsh
