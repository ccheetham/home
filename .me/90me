prepend_path $ME/bin
prepend_path $ME_ARCH/bin

# setting LD_LIBRARY_PATH seems to cause some security concerns in OS X shells
if [[ $ME_OSNAME != osx ]] ; then
  export LD_LIBRARY_PATH=$ME_ARCH/lib
fi

MANPATH=$ME_ARCH/share/man:$MANPATH

# vim: ft=zsh
