# =============================================================================
# GNU (et al) toolchain
# =============================================================================

export PKG_CONFIG_PATH=$ME_DISTRO_DIR/lib/pkgconfig:$PKG_CONFIG_PATH

if [[ osx == $ME_OS ]] ; then
    alias ldd='otool -L'
fi

# vim: ft=zsh
