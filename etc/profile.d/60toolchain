# =============================================================================
# GNU (et al) toolchain
# =============================================================================

export PKG_CONFIG_PATH=$ME_DISTRO_TOOL_DIR/lib/pkgconfig

if [[ osx == $ME_OS ]] ; then
    alias ldd='otool -L'
fi

# vim: ft=zsh
