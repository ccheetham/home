# =============================================================================
# Kubernetes
# =============================================================================

if command -v microk8s.kubectl >/dev/null; then
  alias kubectl='microk8s.kubectl'
fi

k() {
  local cmd="kubectl $*"
  echo "\$ "$cmd
  eval $cmd
}

# vim: ft=zsh
