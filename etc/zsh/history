mkdir -p $ME_ZSH_CACHE_DIR
HISTFILE=$ME_ZSH_CACHE_DIR/history
HISTSIZE=10000                          # lines to load at start ...
SAVEHIST=10000                          # ... lines to save on exit

unsetopt hist_fcntl_lock

# vim: ft=zsh
