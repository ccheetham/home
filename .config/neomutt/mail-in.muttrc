set folder      = $ME_MAIL_DIR/$INSTANCE
set mbox_type   = Maildir
set mbox        = "+received"
set postponed   = "+drafts"
set record      = "+Sent"
set spoolfile   = "+INBOX"

# vim: filetype=muttrc
